{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Ender Client window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "dialog:allow-open"
  ]
}
